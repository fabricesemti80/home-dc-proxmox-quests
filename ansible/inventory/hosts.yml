---
all:
  children:
    proxmox_guests:
      children:
        proxmox_debian_guests:
          hosts:
            proxmox-pbs-0:
              ansible_host: 10.0.40.16
              ansible_user: root
              ansible_ssh_private_key_file: ~/.ssh/fs_home_rsa
              guest_kind: lxc
            docker-svc-0:
              ansible_host: 10.0.40.54
              ansible_user: fs
              ansible_ssh_private_key_file: ~/.ssh/fs_home_rsa
              guest_kind: vm
              docker_users:
                - fs

        proxmox_backup_servers:
          hosts:
            proxmox-pbs-0:
              pbs_datastore_mount_strategy: nfs

    docker_standalone_hosts:
      children:
        portainer_server_hosts:
          hosts:
            docker-svc-0:
        portainer_agent_hosts:
          hosts:
            docker-svc-0:
            # FIXME: add miniPC host once details known
            # mini-pc-0:
            #   ansible_host: REPLACE_ME
            #   ansible_user: REPLACE_ME
            #   ansible_ssh_private_key_file: ~/.ssh/fs_home_rsa
